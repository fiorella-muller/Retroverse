import React from 'react'
import ReactDOM from 'react-dom/client'
import RetroVerseApp from './RetroVerseApp.jsx'

ReactDOM.createRoot(document.getElementById('root')).render(
  <React.StrictMode>
    <RetroVerseApp />
  </React.StrictMode>,
)
